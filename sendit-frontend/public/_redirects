# Redirect www to non-www
https://www.senditsystem.netlify.app/* https://senditsystem.netlify.app/:splat 301!
http://senditsystem.netlify.app/* https://senditsystem.netlify.app/:splat 301!

# Prerendered static HTML files are served first by Netlify.
# Everything else falls back to the SPA shell.
/* /index.html 200
